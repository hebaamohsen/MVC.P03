@model List<UserInRoleViewModel>
@{
    ViewData["Title"] = "AddOrRemoveUsers";
    var RoleId = ViewBag.RoleId;
}

<h1>AddOrRemoveUsers</h1>
 <form class="my-3" asp-action="AddOrRemoveUsers" asp-route-roleId="@RoleId">
     @for(var i=0; i<Model.Count; i++)
    {
        <div class="form-group">
            <label asp-for="@Model[i].IsSelected" >@Model[i].UserName</label>
            <input asp-for="@Model[i].IsSelected"  />
            
        </div>

        <input type="hidden" asp-for="@Model[i].UserId"/>
        <input type="hidden" asp-for="@Model[i].UserName" />
    }
    
          <div class="my-3">
        <input type="submit" value="Update" class="btn btn-success" />
        <a class="btn btn-light" asp-action="Update" asp-controller="Role" asp-route-id="@RoleId">Back To List</a>
          </div>
           
        </form>

